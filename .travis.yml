language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-pandora' ]; then cd .. && eval "mv $currentfolder generator-pandora" && cd generator-pandora; fi

